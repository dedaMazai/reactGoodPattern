export enum AppRouters {
    MAIN = 'main',
    FORBIDDEN = 'forbidden',
    // last
    NOT_FOUND = 'not_found'
}

export const getRouteMain = () => '/';
export const getRouteForbidden = () => '/forbidden';
